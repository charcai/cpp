#include <bits/stdc++.h>
using namespace std;

int n, m, u, v;

int main()
{
	freopen("graph.in", "r", stdin);
	freopen("graph.out", "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= m; i++)
		cin >> u >> v;
	puts("No");
	return 0;
}
